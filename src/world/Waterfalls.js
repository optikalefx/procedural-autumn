// Waterfall sheets, spray, mist and plunge pools.
// Stub — owned by a dedicated system author. See docs/DESIGN_BRIEF.md.
import { System } from '../core/System.js';

export class Waterfalls extends System {
  constructor(ctx) {
    super(ctx);
    this.name = 'Waterfalls';
  }

  async init() {}

  update(dt, elapsed) { void dt; void elapsed; }

  dispose() {}
}
